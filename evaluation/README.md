# QuantNova AI Evaluation
Run all cases in `questions.json`. Record answer, citations, retrieved sources, tool trace, latency, model ID, index version, top-k, and pass/fail labels for groundedness, citation accuracy, tool selection, workflow completion, and safe handling.

Ablation: run the same set with top-k 3 and top-k 5.
